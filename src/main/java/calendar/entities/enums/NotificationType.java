package calendar.entities.enums;

public enum NotificationType {
    EVENT_CHANGED,
    INVITE_GUEST,
    UNINVITE_GUEST,
    USER_STATUS_CHANGED,

    CANCEL_EVENT,
    UPCOMING_EVENT,

    REGISTER, // not in requirements
    USER_ROLE_CHANGED, // - not in requirements
}
